import { FC } from 'react';
import LoginForm from '../../components/LoginForm';

const AdminLogin: FC = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default AdminLogin;
